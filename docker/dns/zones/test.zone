$ORIGIN test.
$TTL 60
@       IN      SOA     dns.test. admin.test. (
                        2024010101 ; serial
                        3600       ; refresh
                        1800       ; retry
                        604800     ; expire
                        86400      ; minimum
                        )
        IN      NS      dns.test.

dns     IN      A       _NETWORK.0.254
